# Crystall 
## Description
Crystall is a UCI-supporting chess engine. It is written in C++ and developed by @GoobusTheNoobus.

## Building
Crystall uses makefiles to compile its code. Navigate to project root, and use `make` to build into an executable `crystall.exe`


